# dsh-skin-claude-code bundle patch: inserts the dual-face plugin row into the
# web profile roster. Applied as a profile bundle layer via the `dsh.bundle.patch`
# manifest field — `dsh plugin add` auto-registers the bundle into
# dsh.profile.bundles, so no manual cordis.patch.yml editing is required.
- insert:
    - id: claude-code-skin
      name: dsh-skin-claude-code
